extends layout

block content
  .main.container
    .row.text-xs-center
      .col-md-6.col-md-offset-3.p-t-2
        img(src='/images/info.svg')        
        p= message 
        br
        a(href='/') Back to Jobs Board
        